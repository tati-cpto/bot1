# sync-worker

Scheduled background worker (Node 18+).

```bash
npm ci
node worker.js
```

Configuration is read from environment variables, provided through
repository Actions secrets and variables.
